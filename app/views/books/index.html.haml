%h1 Books!
- if @books.size > 0
  %ul
    - @books.each do |book|
      %li
        = link_to book.title, book_path(book)
        |
        = link_to "Edit", edit_book_path(book)
        |
        = link_to "Delete", book_path(book), :method => :delete, :confirm => "Are you sure?"

  = link_to "Create book", new_book_path
- else
  You have no books!
  = link_to "Create one!", new_book_path
